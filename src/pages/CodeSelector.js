import React from 'react';

import CodeSelectorComponent from '../components/auth/components/CodeSelector';

const CodeSelector = props => {
  return <CodeSelectorComponent {...props} />;
};

export default CodeSelector;
